<!DOCTYPE html>
<html>
<head>
    <title><%= algo.name %> - Results</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body class="bg-light">

<div class="container mt-4">

    <a href="/" class="btn btn-secondary mb-3">⬅ Back</a>
    <h2><%= algo.name %> - Performance Results</h2>

    <!-- Accuracy -->
    <h4 class="mt-4">Accuracy</h4>
    <p class="lead"><strong><%= metrics.accuracy %></strong></p>

    <!-- Classification Report -->
    <h4 class="mt-4">Classification Report</h4>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Class</th>
                <th>Precision</th>
                <th>Recall</th>
                <th>F1-Score</th>
                <th>Support</th>
            </tr>
        </thead>
        <tbody>
            <% ["Low", "Medium", "High"].forEach(cls => { %>
                <tr>
                    <td><%= cls %></td>
                    <td><%= metrics.classification_report[cls].precision.toFixed(2) %></td>
                    <td><%= metrics.classification_report[cls].recall.toFixed(2) %></td>
                    <td><%= metrics.classification_report[cls]["f1-score"].toFixed(2) %></td>
                    <td><%= metrics.classification_report[cls].support %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <!-- Confusion Matrix -->
    <h4 class="mt-5">Confusion Matrix</h4>
    <img src="<%= confusionImage %>" class="img-fluid shadow rounded">

    <!-- ROC Curve -->
    <h4 class="mt-5">ROC Curve</h4>
    <img src="<%= rocImage %>" class="img-fluid shadow rounded">

</div>

</body>
</html>
